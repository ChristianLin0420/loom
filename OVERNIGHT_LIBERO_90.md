# LOOM LIBERO 90% overnight ledger

## Completion gate

- Average standard-LIBERO success rate: at least **90.0%**.
- Authoritative evaluation: real checkpoint policy, all four suites, 10 tasks per suite, 10 episodes per task, three seeded decoder-noise replicates (**1,200 episodes / 400 environment conditions**), zero errors.
- Every method change remains operator-native: persistent belief, sparse operator coefficient, shared affine operator bank, and embodiment-specific action realization.
- Inputs remain RGB, proprioception, and language only. No privileged simulator state or task-specific controller is introduced.
- A checkpoint is evaluated only after its configured training objective has converged off known degenerate floors.

## Starting point

| Checkpoint | Spatial | Object | Goal | Long | Average | Episodes | Errors |
|---|---:|---:|---:|---:|---:|---:|---:|
| R0-A align-flip, step 25,000 | 33.33 | 26.00 | 32.00 | 15.00 | 26.58 | 1,200 | 0 |
| R0-A sparse-CE proposal refinement, step 27,000 | 44.67 | 34.33 | 41.33 | 25.33 | **36.42** | 1,200 | 0 |
| R0-A deployed-coefficient refinement, step 49,666 | 40.67 | 29.67 | 53.67 | 25.00 | **37.25** | 1,200 | 0 |

Final step-27k replicate SRs: 37.00%, 34.00%, and 38.25%.

## Method-wise progress to the starting point

| Method / correction | Training or tuning change | LIBERO average | Protocol / decision |
|---|---|---:|---|
| Original R0-A | belief-conditioned decoder, original latent initialization | 8.42% | Full 1,200; superseded |
| Learned initial belief | learned `z0` | 12.83% | Full 1,200; kept in later recipe |
| Three implementation fixes | proprio-conditioned 8-step decoder, active dynamics negatives, corrected balance | 2.20% / 1.00% | Full 1,200 ctrl / learned-`z0`; insufficient alone |
| Action-anchor alignment | reverse the existing alignment so `q_delta` tracks detached action-labelled `q_a` | 24.58% @7.5k | Full 1,200; kept |
| Longer action-anchor training | continue the same method to 25k | 26.58% | Full 1,200; prior baseline |
| Hard PL continuation | unchanged hard support objective past its reproducible collapse | 5.75% | Full 1,200; rejected |
| Dense-logit KL proposal | temperature/LR variants | 7.75–11.50% | 400-condition selection; rejected |
| Sparse weighted CE | temperature 1.0; proposal LR 0.3; proposal-only continuation | 39.00% selection / **36.42% full** | Kept; prior verified best |
| Deployed-coefficient action exposure | train existing `L_act` on exact deployed `c_pi`; decoder/proposal only; converged @49,666 | 38.00% selection / **37.25% full** | Highest measured full average; failed the predeclared selection gate |

The implementation/error corrections remain in the recipe. Hyperparameter changes were kept only when an end-to-end gate improved; inference-only dwell, averaging, duration normalization, and unrestricted continuation did not.

## Iteration 0 — stable proposal supervision

### Proposal

Replace discontinuous hard-support Plackett–Luce imitation with coefficient-weighted sparse cross-entropy, and refine only `pi_c` against the fixed action operator encoder.

### Minimal modification

- Proposal target: detached top-4 `q_a` coefficient weights.
- Temperature: 1.0.
- Proposal LR scale: 0.3.
- Frozen during refinement: estimator, operator bank, `q_delta`, `q_a`, decoder, and EMA estimator.
- Training interval: step 25,000 to 27,000 using the original uniform-window distribution.

### Result

- Seeded 400-condition selection gate: 39.00%.
- Authoritative full evaluation: **36.42% (437/1,200)**, +9.83 points over the valid 25k baseline.
- Consolidated checkpoint passed complete structural and numeric verification.

### Decision

Keep. Dense-logit KL, unrestricted joint continuation, duration-normalized execution, decoder averaging, and gripper dwell were rejected by end-to-end SR gates.

### Retrospective convergence amendment

This result predates the overnight convergence-only evaluation rule. The checkpoint is structurally and numerically verified, but its short proposal-only continuation was still moving: over four 500-step blocks, total loss drifted 3.24%, dynamics 3.59%, and sparse proposal CE 5.99%. The exact verdict was `NOT_CONVERGED`; therefore 36.42% is the authoritative starting measurement, not a claim that the 27k training objective converged.

- Source run/config hash: `r0a_s8_sparse_lr03_prop_flat`, `1fe35d9be2ab97a0`.
- W&B run ID: `a8a4f825e1224bff`.
- Checkpoint verification: `runs/r0a_s8_sparse_lr03_prop_flat_eval/verify_000027000.json` (`pass: true`).
- Full result: `runs/eval_final_r0a_sparse_refine_flat_s27000_plain_seeded1200/summary.md`.

## Iteration 1 — deployed-coefficient action refinement

### Proposal

Close the training/deployment coefficient gap inside the existing R0 objective. Historically `D_e` learned `L_act` only from the action-conditioned teacher `c_a = q_a(a,z)`, but evaluation supplies `c_pi = top4(pi_c(z,l))`. Train the same decoder loss on `c_pi` while retaining sparse-CE anchoring to `c_a`.

### Minimal modification

- Added `losses.act.decode_from`, with the historical `q_action` path remaining the default.
- The new `proposal` path computes the exact deployed top-4 support and restricted-softmax weights, with a straight-through gradient into `pi_c`.
- Inputs are unchanged: RGB, proprioception, and language.
- No module or loss was added. The decoder remains `D_e(proprio,c)`, so task information cannot bypass the operator coefficient.
- Refinement updates gradients only in the existing proposal and decoder; estimator, bank, and both coefficient encoders remain frozen. The inherited loop still moves the non-gradient EMA target toward the fixed estimator, but EMA does not feed either enabled trainable path and is not used by R0 evaluation.
- Configuration: `configs/r0a_deploy.yaml`, continuing from the verified best-scoring (not converged) step-27k checkpoint on a 50k cosine horizon. Proposal LR scale 0.05; decoder LR scale 0.3.

### Convergence gate

Training continues until deployed `act/decode` and sparse proposal CE plateau off degenerate floors. Only then is an end-to-end evaluation submitted.

Predeclared deploy-stage gate: start after the 27k initialization, four complete non-overlapping 2,000-step blocks, at most 2% relative drift for both `act/decode` and `loss/proposal`. Sparse CE must remain below its uniform `ln(128)=4.852` floor. Frozen `act/align` and `delta_sel` are excluded; deployment coefficient L2, top-4 overlap, and gradient norm remain watch metrics. The earliest valid verdict is step 35k and the hard schedule limit is step 50k.

### Launch manifest

- Initialization: all 16 verified FSDP shards at step 27,000 from `r0a_s8_sparse_lr03_prop_flat_eval/shards_000027000`.
- Resolved config: `configs/r0a_deploy.yaml`; config hash `a199324a6205bb6d`; git `d499d57` with the recorded dirty method/eval changes.
- Data: cached LIBERO RGB features, proprioception, and language; original uniform-window sampler; batch 8/GPU × 16 GPUs.
- Optimization: AdamW/cosine horizon 50k, base LR `3e-4`, decoder LR scale `0.3`, proposal LR scale `0.05`; all other gradient modules frozen. Future frozen-coordinate stages must also suppress the loop's unconditional EMA update.
- Losses: existing deployed-coefficient `L_act` plus sparse weighted proposal CE; dynamics and balance disabled for this refinement.
- Planned end-to-end gate after convergence: all four suites, 10 tasks/suite, 10 episodes/task, seeds 0/1/2, one decoder sample, gripper dwell off, duration normalization off; 1,200 episodes total.
- Evaluation selection is predeclared: first run the matched seed-0 400-condition arm with common decoder noise. Promote to the authoritative 1,200 only at least 41.0% (versus the matched 39.0% starting arm), with paired new-only wins greater than old-only wins and no suite regressing by more than five points.

### Status

Implementation and full repository tests passed. Launched as `r0a_deploy_s1` on 2026-08-19 UTC: Slurm links `32475631`, `32475632`, `32475633`; W&B run ID `e96baaa9803b41f3`. Detached tmux session `loom_autostop_r0a_deploy_s1` checks the declared gate from step 35k and writes `STOP` only on a clean `CONVERGED` verdict.

Launch correction: links `32475631` and `32475632` exited with infrastructure code 13 before starting a training task. Link `32475633` restored all 16 shards but failed before its first optimizer step because the inherited diagnostic probe called `retain_grad()` on frozen `q_delta` logits. The minimal implementation fix now installs that probe only when the logits require gradients; the deploy-path regression test passes. Recovery links `32475725` and `32475726` were queued against the unchanged step-27k initialization. This is an execution fix, not a method change.

First eligible convergence check, step 35,283: `NOT_CONVERGED`. Sparse proposal CE was flat (0.43% drift), but deployed `act/decode` still improved beyond tolerance (8.12% drift over the four rolling 2k blocks). Training therefore continued and no evaluation was submitted.

Final convergence: autostop fired at step 49,663 on 2026-08-19 16:33 UTC. Across the final four 2,000-step blocks, `act/decode` drift was 1.785% and sparse proposal CE drift was 0.662%; sparse CE remained off its uniform floor. Verdict: `CONVERGED`. The primary job `32475725` exited 0 at step 49,665; its serialized recovery link observed `STOP`, saved step 49,666, and also exited 0. The final checkpoint contains all 16 shards and still returns `CONVERGED` (1.910% / 0.647% drift). Evaluation may now proceed from step 49,666 after consolidation verification.

Consolidation verification passed for `runs/r0a_deploy_s1_eval/ckpt_000049666.pt`: 923 keys, no missing, unexpected, shape-mismatched, or numerically mismatched tensors, and a real non-stub policy reconstructed at global step 49,666. Report: `runs/r0a_deploy_s1_eval/verify_000049666.json`. The matched seed-0 400-condition selection evaluation ran as Slurm job `32486118`, with one decoder sample, no gripper dwell, no duration normalization, and the recorded per-episode decoder seed scheme.

Selection result: **38.00% (152/400)**, suites spatial/object/goal/long = **40/32/48/32**, zero errors. The prior matched arm was **39.00% (156/400)** with suites 45/40/44/27. All 400 condition, environment-seed, and policy-seed keys match exactly. Paired outcomes were 86 both successful, 66 new-only, 70 old-only, and 178 neither. Iteration 1 is therefore a parity/no-promotion result as a standalone deployment method: average changed by -1 point instead of at least +2, new-only did not exceed old-only, and object regressed by 8 points. Goal and long improved by 4 and 5 points, showing that deployed-coefficient exposure redistributes task success but does not produce a robust policy improvement. Step 27k's 36.42% full result remains authoritative. The converged step-49,666 checkpoint is retained only as the frozen initialization for Iteration 2, not as an improvement. Parity artifact: `runs/select_seeded400/r0a_deploy_s1_s49666_d1_s1/results.json`.

Consolidated seed artifact: `runs/r0a_deploy_s1_eval/ckpt_000049666.pt`. Verification report `verify_000049666.json` passes structure (923 keys, no missing/unexpected/shape mismatch), numeric reconstruction (3,920 replicated tensors and 10,808 sharded pieces, zero mismatches), and real-policy loading (`is_stub=false`, correct estimator/proposal/decoder tensor counts, step 49,666).

### Authoritative result and decision

The full jobs `32486531`, `32486532`, and `32486533` were submitted concurrently before the seed-0 selection gate completed; they do not retroactively make that failed gate pass. They nevertheless form a valid full measurement: all exited 0, loaded the real non-stub step-49,666 checkpoint, and each produced exactly 400 unique episodes with zero errors. Combined suites spatial/object/goal/long were **122/300 (40.67%)**, **89/300 (29.67%)**, **161/300 (53.67%)**, and **75/300 (25.00%)**; overall **447/1,200 (37.25%)**. Every individual seed was 149/400 (37.25%). Artifact: `runs/eval_r0a_deploy_s1_s49666_seeded1200_v2/summary.md`.

This full result supersedes the selection-only decision as the authoritative measurement. Relative to step 27k, average SR improves by **0.83 points**, driven by Goal (+12.34), while Spatial (-4.00), Object (-4.66), and Long (-0.33) regress. Keep step 49,666 as the current best average and the frozen seed for Iteration 2; closing the deployed coefficient gap alone remains insufficient and leaves **52.75 points** to the 90% completion gate.

## Iteration 2 — B4 recurrent burn-in proposal refinement

### Proposal

Expose only the existing proposal to the recurrent belief distribution used during evaluation. Initialize each unchanged five-state supervised window with four real prior operator-boundary observations from the verified, converged step-49,666 deployed-coefficient checkpoint.

### Minimal modification

- For a retained window beginning at canonical time `t`, read the four already-cached operator-boundary observations at `t-32`, `t-24`, `t-16`, and `t-8` (one boundary every eight control steps), oldest to newest. No pixels are re-encoded and no new model input is introduced.
- Replay those four observations independently through the online estimator and the EMA target estimator under `no_grad`, each starting from its own empty recurrent state. Do not reuse the online prefix belief for the EMA path.
- Detach both final prefix beliefs, then use them only to initialize the existing five main states. The five supervised observations, action chunks, targets, horizon indexing, deployed `L_act`, and sparse proposal CE remain unchanged.
- Update only `pi_c`. Hold the estimator, decoder, operator bank, `q_delta`, and `q_a` fixed. The EMA target is also explicitly frozen: it receives neither gradients nor parameter updates.
- This is a data-context correction, not an architecture extension: no new input modality, module, or loss is added.

### Training and convergence gate

- Frozen seed: `runs/r0a_deploy_s1_eval/ckpt_000049666.pt`, global step 49,666. Its authoritative full result is 37.25%; the earlier matched selection arm was 38.00%.
- Sampling remains `uniform_window`; optimization continues on a fixed 80,000-step cosine horizon with proposal LR scale 0.05 and proposal as the only trainable module. The first link stops at step 58,000 without shortening that horizon.
- Convergence starts at step 49,666 and requires four complete, non-overlapping 2,000-step blocks with at most 2% relative drift for both deployed `act/decode` and `loss/proposal`, off their applicable degenerate floors.
- Stability alone is insufficient: the final block median `act/decode` must also improve by at least 5% relative to the first post-49,666 block median. This manual, config-hashed efficacy gate rejects a flat objective that never benefits from recurrent context.
- The earliest eligible verdict is step 57,666. Submit no simulator evaluation unless both the clean `CONVERGED` verdict and the efficacy gate pass.

Held-out preflight: `logs/recurrent_suffix_parity_s49666.json` compares B4 with full episode history at 116 cached boundaries across all 40 tasks and recurrent ages 6, 12, 24, and 32. Top-1 and top-4 support agree exactly in every case; overall action-loss parity is 0.999996 and the worst per-age parity is 0.999866. The minimum-context gate therefore passes. Re-run this parity check after training because `pi_c` changes even though the estimator stays fixed.

Launch recipe identity: `configs/r0a_burnin.yaml`, resolved config hash `216be2c169a355b6`; initialize a new run from all 16 verified step-49,666 shards while preserving global step and Adam moments.

Implementation and the stable full repository suite passed. `runs/r0a_burnin_s1` was seeded with 16 hard-linked, nonzero step-49,666 shards and an independent `LATEST`. Two serialized four-hour links, jobs `32490962` and `32490963`, were submitted on 2026-08-19 UTC with the fixed 80k scheduler and link-local `--stop_at 58000`. No B4 end-to-end result is claimed until the training gates pass.

Startup verification: all 16 distinct ranks restored step 49,666 on A100 GPUs with the intentional config-hash transition `a199324a6205bb6d -> 216be2c169a355b6`. Cached B4 data and the first optimizer steps are healthy; only `pi_c` reports gradient norm. W&B run ID `beb1b658a8594b9e`, URL `https://wandb.ai/crlc112358/loom/runs/beb1b658a8594b9e`.

### Result and decision

Jobs `32490962` and `32490963` completed 0:0 at step 58,000. The run contains 8,334 contiguous post-seed updates, all 16 terminal shards, zero skipped/nonfinite/error steps, EMA fixed, and proposal as the only module with gradients. The exact config-resolved four-block verdict was `NOT_CONVERGED`: `act/decode` medians 0.018244/0.018445/0.019099/0.019203 drifted 4.995%, while proposal CE 1.60927/1.61030/1.61687/1.61605 was flat at 0.470% drift and stayed far from its uniform floor. Against the locked first block (0.018161), final action loss worsened 5.736% instead of improving at least 5%.

Final negative outcome: reject B4. It is a faithful and stable replay implementation, but recurrent exposure alone does not improve the deployed operator policy. Per the predeclared gate, no checkpoint was consolidated and no simulator evaluation was run. The highest measured full result remains the verified step 49,666 at 37.25%; proceed to action-anchored bank fitting rather than tuning burn-in length or LR after the fact.

## Iteration 3a — action-anchored operator-bank fit

### Seed, data history, and objective

- Seed a new run from the verified consolidated artifact `runs/r0a_deploy_s1_eval/ckpt_000049666.pt` at global step 49,666. **Do not seed from B4:** its step-58,000 shards failed the declared gate and were deliberately not consolidated.
- Retain `uniform_window` and B4's real cached history. For every retained window, prepend the four operator-boundary observations at `t-32`, `t-24`, `t-16`, and `t-8`; replay them oldest to newest through the online estimator and EMA target separately under `no_grad`, each from its own empty recurrent state; detach both prefix beliefs before the unchanged five-state window. No pixels are re-encoded and the observations, expert action chunks, and targets are unchanged.
- Reserve the complete `demo_49` trajectory from each of the 40 LIBERO tasks for this stage's offline gate. Bank optimization uses the exact 1,960-trajectory complement; the gate loader switches the authenticated selector from `train` to `gate` and refuses an incomplete cache instead of falling back to training windows.
- For each of the four expert segments, compute `c_a,h = sg(q_action(a_h, z_h))`. Feed this action-labelled coefficient sequence, not `c_delta`, to the existing sequential `L_dyn` rollout and its same-trajectory hinge at horizons 1–4. This changes the coefficient anchor for dynamics; it adds no model input, module, or loss.
- Train the existing operator bank only. Freeze the online estimator, EMA estimator, `q_delta`, `q_action`, decoder, `pi_c`, and `Phi`; disable every loss except `L_dyn`; and disable EMA updates as well as EMA gradients so both belief coordinate systems remain exactly at step 49,666.
- Preserve global step and the fixed absolute 80,000-step cosine horizon. This is not an 80,000-update restart: the stage begins at 49,666 and may run only through step 80,000.

### Convergence and offline promotion gates

- Convergence is measured from step 49,666 over four complete, non-overlapping 2,000-step blocks. The only primary is `loss/dyn`, whose block medians must have at most 2% relative drift. Watch, but do not average into that verdict, `dyn/pos`, `dyn/neg`, `delta_op`, `delta_sel/h1` through `delta_sel/h4`, and `grad_norm`. The earliest eligible convergence verdict is step 57,666.
- A clean convergence verdict is necessary but not sufficient. Select a fixed, seeded, task-balanced set of 256 stage-heldout windows without replacement and draw 32 proposal plans per root with a seed derived from that window's fixed ordinal. Compute `delta_sel` against the previous row in one global cyclic 256-row order, independent of runtime batch size. Use 2,000 shared-resample, task-balanced whole-trajectory-cluster bootstrap replicates at seed 0 to form 95% confidence intervals. Promote only if (1) the lower bound for `delta_sel/h` is greater than zero at every horizon 1–4, (2) the lower bound for `identity_error/h - bank_rollout_error/h` is greater than zero at every horizon 1–4, and (3) the lower bound for proposal-candidate leaf pairwise cosine-distance spread is greater than zero. There is no additional numerical margin: each lower confidence bound must simply be strictly positive.
- **Do not run a direct end-to-end LIBERO evaluation for Iteration 3a.** The direct R0 path is `pi_c -> D_e` and does not call the bank, so direct success rate cannot evaluate a bank-only change. Passing these gates promotes the bank to the operator-oracle/search stage; failing either gate rejects Iteration 3a without a simulator claim.

### Implementation and launch preflight

The resolved recipe is `configs/r0a_bank_ca.yaml`, config hash `6db3572a8ee3123e`, at git `d499d57` with the shared worktree recorded as dirty. The action-labelled route is explicit and fail-closed: `q_action` is evaluated once per horizon under `no_grad`, its coefficient is detached, and that same sequence reaches `L_dyn`, `delta_op`, and every `delta_sel/h`; missing action labels raise instead of falling back to `q_delta`. Only the bank is optimizer-trainable, and `optim.reset_state_modules: [bank]` clears its old `q_delta`-objective Adam state exactly once on the seed-hash transition while retaining global step, the 80k scheduler, sampler, model/EMA, all non-bank state, and same-hash requeue moments.

The deployed cache and raw discovery agree on all 2,000 demonstrations. With burn-in 4, the authenticated train split is 1,960 trajectories / 47,271 windows / 40 tasks (`sha256:f61c453864dc8a84e274a65e834e037a83ef8407ed4e9635f84c78d814fe2e7e`); the gate split is 40 complete trajectories / 918 windows / 40 tasks (`sha256:6d933080a8c902048a50ef2a4805d98318b6ff233f909716eee2086a27886057`). They are disjoint and their union is the full cache. The fixed 256-row gate covers every heldout trajectory with 6–7 unique windows and no repeats. `scripts/bank_ca_gate.py` authenticates the consolidated checkpoint/config/cache/trajectory manifest/exact selected order, uses ordinal-stable proposal RNG and cyclic comparison context independent of runtime chunking, clusters the paired bootstrap by whole trajectory and task, and writes a fail-closed atomic report.

Preflight evidence: the gate-focused suite passes 31/31; the selective-reset/action-routing slice passes; the complete repository suite `pytest -q -m 'not slow and not gpu and not benchmark'` passes; direct CLI import/compile and `git diff --check` pass. No simulator result is implied by these tests.

### Downstream only after Iteration 3a passes

Do not treat the historical 79.17% K=16 oracle as a current operator ceiling: it used step 25k and confounded operator choice with decoder-noise variation. Remeasure on the promoted Iteration 3a checkpoint with identical decoder noise across all candidates and the realizability mask. If oracle@16 is below 90%, collect real LIBERO failure-snapshot groups: direct argmax plus 15 sampled root operators, identical simulator/policy state and decoder noise per branch, one forced operator followed by the frozen direct policy, and terminal success as the only reward. Train only `pi_c` with actual-outcome clipped GRPO plus the already-allowed sparse `L_proposal` and `1e-2 L_balance`; never use `Phi` as its own reward.

The canonical causal gate is `scripts/libero_root_oracle.py`. Only after a matching action-bank heldout PASS, it runs 16 fresh branches from each official seed-0 WorkItem's identical reset: arm 0 forces exact proposal argmax once, arms 1–15 force SHA-seeded proposal samples once, all arms use identical root decoder noise, and the exact decoded/executed segment is scored by `q_action`. Only the root is intervened on; every later replan is the frozen direct argmax policy. Eligibility is `||q_a(a_root,z_root)-c_root||_2 <= 0.5`, reward is terminal LIBERO success, and no rejected arm receives fall-through credit. Promotion requires 400 complete groups / 6,400 rows / zero errors, exact arm-0 reproduction of the authoritative seed-0 149/400 outcomes (40/32/48/29), every reset/RNG/root/execution/continuation witness, and masked oracle at least 360/400 overall. Its immutable resume identity includes a deterministic 14-file behavior-source closure, and workers/finalization fail closed if any source is missing or changes. This measures a causal root action-value question and supplies correctly paired failure-snapshot/GRPO labels; it does not claim to evaluate the bank/Phi planner itself. Root/oracle/eval validation passes 137/137; launch remains blocked on a promoted bank artifact and PASS report.

`scripts/operator_oracle.py` is a separate optional **16-policy stochastic-portfolio diagnostic**, not the canonical operator oracle or a promotion gate. Its arms resample at every replan and therefore occupy different states after the first root; max terminal success across those full policies is not 16 alternatives at one planning belief and is not comparable to LOOM shooting, which samples depth-4 sequences from one belief, rolls them through the bank, scores leaves with `Phi`, and executes one root. Keep the 19,200-rollout diagnostic unlaunched unless that distinct portfolio question later becomes useful.

Only after the common-noise, post-realizability operator oracle reaches at least 90% may the existing `Phi` be trained. Collect depth-4 operator sequences from identical snapshots, label terminal success, and train only the existing `L_Phi = MSE(Phi(bank.rollout(c_seq,z),lang), success)`. Deployed search remains the original contract: N=1000, depth 4, `Phi` leaf score only, execute the selected root, no MCTS/uncertainty/cost term/new selector. Before E2E, held-out `Phi` selection must beat direct by at least 10 points, capture at least 60% of oracle headroom, keep every suite, preserve oracle >=90%, and have <1% realizability exhaustion.

Launch record (2026-08-19 UTC): `runs/r0a_bank_ca_s1` was seeded with all 16 nonzero step-49,666 shards. Serialized Slurm jobs `32508238` and `32508239` were submitted; `32508238` is active and `32508239` is dependency-held. All 16 A100 ranks restored the intended seed and reset exactly the three bank optimizer states once across the config transition `a199324a6205bb6d -> 6db3572a8ee3123e`. The live loader authenticates the 1,960-trajectory train split, EMA updates are off, only `gnorm/bank` is present, gradients are finite, and skipped updates remain zero. W&B run `b5312332088247d9`: `https://wandb.ai/crlc112358/loom/runs/b5312332088247d9`. Autostop is armed for the earliest eligible convergence step 57,666. The near-zero initial operator gaps are the reset-seed baseline. The fixed early liveness verdict is taken once at step 52,666 over updates `(50,666, 52,666]`: median signed `delta_op` must exceed `+0.01`, median finite `gnorm/bank` must exceed `1e-4`, and skipped updates must remain below 1% of the window. Failure rejects the stage and writes `STOP`; these thresholds will not be tuned after observing the window.

Liveness outcome: **FAIL / reject Iteration 3a.** The fixed `(50,666, 52,666]` window contains exactly 2,000 contiguous rows. Median signed `delta_op` was `+2.79918e-5` (mean `+3.98123e-5`, maximum `+0.005389`), versus the locked requirement `>+0.01`; no row cleared the threshold. Bank gradient median was `0.00653263`, skipped-update rate was zero, and per-horizon `delta_sel` medians were positive but only `2.43e-5/3.45e-5/3.59e-5/5.02e-5`. Thus wiring and optimization were live, but the bank did not become operator-discriminative. Artifact: `runs/r0a_bank_ca_s1/liveness_050666_052666.json`. `STOP` was written at 2026-08-19 19:55:48 UTC. Jobs `32508238/32508239` both completed 0:0; the first stopped at step 52,714 and the serialized recovery link restored all 16 shards, preserved the new bank moments, and stopped at 52,715. The terminal checkpoint has 16/16 nonzero shards, but convergence is correctly `TOO_EARLY` with only 3,049/8,000 required post-seed rows. No heldout gate, consolidation, or simulator result is claimed.

## Iteration 3a-N4 — stronger existing same-trajectory discrimination

This is one scalar change from the rejected action-anchored bank recipe: set the already-existing `losses.dyn.neg_weight` from `1.0` to `4.0`. Restart from all 16 verified deploy step-49,666 shards, not the rejected Iteration 3a state; preserve its action-labelled `c_a`, detached/frozen coordinates, bank-only training, authenticated trajectory split, burn-in, bank LR, 80k absolute schedule, one-time bank Adam reset, liveness/convergence windows, and heldout gate exactly. No input, module, loss, negative source, or inference rule changes. The observed hinge median `0.17023` is already close to its zero-gap weighted value `0.1875`, so it is active; increasing its weight amplifies its discrimination gradient, whereas increasing only its margin would mostly add a constant on already-active rows. Fresh bank initialization is not used because the inherited spectral diversity matches a fresh bank.

Recipe: `configs/r0a_bank_ca_n4.yaml`, resolved hash `e2dbdef1d95ad344`; its focused exact-delta tests pass. `runs/r0a_bank_ca_n4_s1` was seeded from all 16 original step-49,666 deploy shards and launched as serialized jobs `32511156/32511157`. All ranks restored the intended seed, cleared only the three bank optimizer states, authenticated the same train split, and began with finite bank-only gradients and zero skips. W&B run `f630e21a0e394e18`: `https://wandb.ai/crlc112358/loom/runs/f630e21a0e394e18`.

N4 liveness outcome: **FAIL / reject.** The exact `(50,666, 52,666]` window again contains 2,000 contiguous rows. Median signed `delta_op` was `+6.02305e-5` (mean `+7.53962e-5`, maximum `+0.005573`, 0/2,000 above `+0.01`), versus the unchanged `>+0.01` requirement. The stronger hinge was active—median `dyn/neg=0.67803` versus `dyn/pos=0.23636`, and bank gradient median doubled to `0.0137725`—with zero skips; per-horizon `delta_sel` medians remained only `5.56e-5/6.71e-5/6.84e-5/8.69e-5`. Therefore greater discrimination weight changed the intended gradient magnitude but still could not make the frozen action code bank-realizable. Artifact: `runs/r0a_bank_ca_n4_s1/liveness_050666_052666.json`. `STOP` was written at 2026-08-19 20:25:55 UTC; the safety link is terminal at step 52,714. No convergence, heldout, consolidation, or simulator result is claimed.

Next single method stage, Iteration 3a-QA: retain the N4 action-labelled dynamics route and hinge, set `losses.dyn.detach_coeff: false`, and train only the existing bank plus `q_action`. The audited recipe also keeps the existing weight-1 `L_act` action anchor active with `align_to: q_a` and `decode_from: q_action` while the decoder is frozen. This is required objective preservation, not a new loss: the q_action implementation defines frozen-D_e reconstruction as q_action's only action-semantic gradient, whereas unanchored `L_dyn` could learn a private bank/code convention. Full q_action scale 1.0 is retained (absolute LR `1.0877e-4` at step 49,666, decaying on the fixed 80k cosine); bank stays at 0.1 scale. Both bank and q_action Adam states reset once on the deploy-to-QA hash transition and survive same-hash requeues; EMA updates remain off and every other module is frozen.

Recipe `configs/r0a_bank_ca_qa.yaml`, resolved hash `0ec8af0a26135ecc`. The fixed early liveness window is exactly updates 50,667–52,666: require median `delta_op > 0.01`, finite median `gnorm/bank > 1e-4` and `gnorm/q_action > 1e-4`, skipped rate below 1%, no unexpected module gradients, and no nonfinite values. Convergence still uses four nonoverlapping 2k blocks from step 49,666 at at most 2% drift, now with both `loss/dyn` and `act/decode` as primaries; the final action-decode block may not worsen against the first complete post-start block. The held-out gate authenticates the exact step-49,666 deploy artifact (`sha256:15f286c268caa5327d5aa3abf1f67ebd0555c426a509fef22cb7f537bf6ab4e1`), verifies estimator/EMA/q_delta/decoder/proposal/potential tensors are unchanged, and uses paired common-noise comparisons requiring the 95% CI lower bound for action-decode improvement versus deploy to be at least zero and the proposal/q_action support-overlap change to be no worse than the predeclared -0.05 non-inferiority margin. The original per-horizon bank rollout, proposal-leaf spread, and `<1%` root-realizability exhaustion gates remain mandatory. The launch-interpreter regression set collects 419 tests and passes with two intentional skips; Python compilation and `git diff --check` also pass.

Launch/result record: fresh run `r0a_bank_ca_qa_s1`, resolved config hash `0ec8af0a26135ecc`; jobs `32515978`, `32516601`, and `32516602` all completed 0:0. All 16 ranks restored the verified deploy step 49,666, made the one declared hash transition `a199324a6205bb6d -> 0ec8af0a26135ecc`, and cleared exactly three bank plus 27 q_action Adam states once. EMA stayed off; the authenticated 1,960-trajectory/47,271-window split was active; only bank and q_action gradients appeared; all rows were contiguous, finite, and unskipped. W&B run `bce708fb85014b5b`.

Liveness outcome: **FAIL / reject QA.** The exact `(50,666, 52,666]` window contains 2,000 contiguous rows. Median signed `delta_op` was `+6.81542e-5`, versus the locked requirement `>+0.01`; median bank and q_action gradients were healthy at `0.0110436` and `0.123302`, skipped rate was zero, and there were no unexpected gradients or nonfinite values. Thus allowing dynamics gradients into an action-reconstruction-anchored q_action did not make the bank/operator pair transition-discriminative. Artifact: `runs/r0a_bank_ca_qa_s1/liveness_050666_052666.json`. `STOP` was written at 2026-08-19 21:28:03 UTC. The three serialized jobs exited cleanly at steps 52,672/52,673/52,674 with 16/16 shards; their one-step sentinel races do not alter the fixed-window verdict. No convergence, heldout gate, consolidation, or simulator evaluation is claimed. Per the predeclared decision rule, stop tuning bank weights or q_action coupling and proceed to the staged fresh-R0 contingency.

## Iteration 3c contingency — staged fresh R0

Use this only if Iteration 3a-QA fails its locked liveness gate; do not tune another bank weight, margin, or LR. A prior true-fresh sparse-CE run already failed representation formation by update 500: `delta_op=-6.3e-6`, q_delta used only seven operators, and coefficient spread fell to 0.074 while proposal CE learned normally. Because sparse proposal CE detaches its belief input, it cannot repair bank/q_delta formation; N1→N4 subsequently doubled the bank gradient without changing that conclusion.

The single staged recipe preserves the current corrected R0 architecture, inputs, 32k cosine, uniform-task sampler, learned initial belief, sparse proposal CE, and spike guard. Phase A (`configs/r0a_fresh_phase_a.yaml`, hash `3de2324b2e369c20`) runs updates 1–2,000 with only the existing `L_act + L_proposal + L_balance`; bank and `L_dyn` are temporarily off so q_delta first learns the action-semantic q_action space. Phase B (`configs/r0a_fresh_phase_b.yaml`, hash `89ba97103054518b`) resumes every state and enables the exact full R0 objective and all six modules through step 32,000; no optimizer state is reset.

Locked Phase-A gate over updates 1,001–2,000: `act/align<0.40`, both coefficient spreads `>0.10`, at least 16 live operators in q_action and q_delta, proposal CE below `ln(128)`, expected finite gradients, no bank gradient, and skips below 15%. Locked Phase-B liveness over updates 2,001–4,000: `delta_op>0.01`, each horizon's `delta_sel>0`, q_delta spread `>0.10` with at least 16 live operators, live bank/q_delta gradients, finite expected modules, and skips below 15%. Failure stops by step 4,000. A survivor must converge over fixed 24k–32k blocks at at most 2% drift in dynamics, action decode/alignment, and proposal loss, retain every liveness condition, then pass the same matched 400-condition ≥41%/paired/no-suite-regression gate before a 1,200-episode evaluation. Expected full cost is about 7.4 hours on 16 A100s; no job is launched while QA remains live.

Phase-A result: **FAIL / reject before Phase B.** Run `r0a_fresh_staged_s1`, Slurm job `32519130`, resolved hash `3de2324b2e369c20`, W&B `17633c551b5a4dfc`, completed 0:0 in 25:07; `LATEST` is step 2,000 with 16/16 shards. The exact 1,000 rows 1,001–2,000 were contiguous, finite, and unskipped; every expected estimator/q_delta/q_action/decoder/proposal gradient was positive, with no bank or unexpected gradient. Gate medians were `act/align=0.592831` (fail, `<0.40`), q_action spread `0.531661` (pass), q_delta spread `0.019616` (fail, `>0.10`), q_action live operators `36` (pass), q_delta live operators `5` (fail, `>=16`), and proposal CE `3.33187` (pass, below `ln(128)`). Artifact: `runs/r0a_fresh_staged_s1/phase_gate_001000_002000.json` (`sha256:b3e81eaf549ce75587dc7bdfac6e067b96fb19f7f54b3de4dd77f108e0f56885`). The fail-closed controller wrote `STOP`; Phase B was never submitted. This closes the offline representation-repair branch. The next method optimizes actual terminal outcomes through the deployed proposal policy instead of further bank/dynamics tuning.

Gate tooling added for this bounded experiment: `scripts/convergence.py` evaluates config-authenticated Phase-A, Phase-B liveness, and terminal windows atomically while preserving the prior QA schema; `scripts/autostop.sh` writes `STOP` on a required-gate failure but never on a Phase-A PASS handoff. The focused convergence suite passes 56/56, the bank-gate suite 60/60, and the fresh/QA config slice 3/3.

## Iteration 4 — terminal-outcome proposal GRPO

All offline bank/code repair arms failed while the direct deployed policy remained reproducible at 37.25%. Iteration 4 therefore optimizes the policy decision that evaluation actually executes: `estimator -> proposal -> decoder`. The bank, q_delta, q_action, decoder, estimator, EMA, and potential are frozen; only the existing proposal is trainable. The allowed R3 objective is clipped GRPO from terminal LIBERO success, anchored by the existing sparse proposal CE on demonstrations and the existing 1e-2 Switch balance. There is no new head, input, reward shaping, Phi reward, search score, q_action rejection, or fallback.

Collection implementation is `loom/eval/outcome_recovery.py` with CLI `scripts/outcome_recovery.py`. Each group shares one reset, environment seed, and decoder RNG stream across eight complete trajectories: arm 0 uses deployed argmax and arms 1–7 sample an exact ordered Plackett–Luce support at every replan. Every sampled decision records detached belief/language, the actual ordered support, coefficient, exact old log-probability, proposal/decoder RNG witnesses, and hashes of both decoded and dispatched action segments. The only reward field is terminal success. Sidecars and their manifest are written atomically and resume only after full identity, hash, tensor, and schema validation. Official init states 0–9 are reserved; immutable training folds `train0..train5` use trials 10–39, and validation uses 40–49. Collector tests pass 17/17 and the broader eval/operator/integration slice passes 154/154.

Collection uses the authenticated step-49,666 checkpoint (SHA `15f286c268caa5327d5aa3abf1f67ebd0555c426a509fef22cb7f537bf6ab4e1`) and behavior-source digest `1622aee21022347a98f82c4e587154f1e13128528441e4d87d96d0d609be1223`. Split `train0` ran as job `32522871`; jobs `32523369/32523370/32523372/32523373/32523374` ran `train1..train5`. All six jobs completed `0:0` with exact COMPLETE manifests: each has 200 groups / 1,600 trajectories, for 1,200 groups / 9,600 trajectories total. Informative-group counts by fold are `146/159/143/148/148/159`, each above the locked independent-group floor of 100. Independent full byte, receipt, and tensor re-authentication passed for all 1,200 groups; manifest SHA-256s for `train0..train5` are `f92f50960e1640b32f2f50c6e9a7c61603204ea21369c6e2493d3770b3683c17`, `8c45d514454598eb1c53c0d3ea3a12b3606f84baae612e5d3c2dc50bfd904421`, `4a53fca9490840e90319d2fde986f8f6c12a6d236869c5000e5b4f0e1555b29b`, `289c0d6796d1bfccf471bb519c3145b0837147280b5059ae62d821a5b7d3594e`, `025a8a81556a733da4401fb489306222e93e72535f3ca8266c4780fc76f9857b`, and `61253c67af3ea3c5cda710a78498e098bd2ea54082ba557ad2145a79e14a1700`. Parent arm 0 succeeded on 463/1,200 (38.6%), at least one of eight arms succeeded on 960/1,200 (80.0%), and 903/1,200 groups (75.2%) had mixed outcomes. This is actionable terminal supervision, not a deployable-oracle or success-rate claim. Official trials 0–9 remain untouched.

The canonical bounded trainer is now `loom/train/outcome_grpo.py`, invoked by `scripts/train_outcome_grpo.py` and `scripts/outcome_grpo.sbatch`, with resolved config `configs/r0a_outcome_grpo.yaml` hash **`1786ff2272566453`**. After the batch-shape authentication correction and process-lifetime sole-writer lock described below, its authenticated 28-file behavior closure is **`61882a886642b0fc2b09ffdaf570ab1f53946f7cf52f4909d900f8368ada01a3`**. The settled tree passes **59/59 focused trainer tests, 75/75 trainer-plus-direct-validation tests, and 117/117 trainer-plus-promotion tests**. It restores the step-49,666 seed, resets only proposal Adam state once, freezes estimator/EMA/bank/q_delta/q_action/decoder/Phi, and performs exactly 800 accepted updates from each of `train0..train5`: 4,800 contiguous proposal-only updates through step 54,466 on the fixed 80,000-step schedule. Each update uses all eight terminal rewards for the group baseline but computes stored-order Plackett–Luce ratios/loss only for arms 1–7, clipping at epsilon 0.2 and reducing equally by replan, arm, then group. Training samples informative groups only, requires at least 100 per fold, uses two contexts per sampled arm, pools Switch balance over all 14 sampled logits, and adds the authenticated train-split sparse q_action CE anchor (1,960 trajectories / 47,271 windows, excluding `demo_49`). There is no Phi/bank/shaped reward; EMA is off and unexpected gradients fail closed. Sampling is deterministic, checkpointable, rank-unique even for nondivisible group counts, and resume reconciles the metrics ledger to the durable checkpoint.

Candidate emission is fail-closed behind both convergence and trust. Convergence evaluates snapshots 53,666/53,866/54,066/54,266/54,466 on all 400 validation groups, including all-equal groups as exact zero, with the exact group-to-arm1..7-to-replan clipped surrogate. A 2,000-replicate seed-0 task-stratified paired whole-group bootstrap must place the 95% CI lower endpoint of final-minus-seed strictly above zero, while every final-minus-earlier 95% CI lies wholly within `[-0.01,+0.01]`; final mean approximate KL must be at most 0.01. The metrics ledger must contain exactly 4,800 contiguous accepted updates, and the last four 200-update sparse-CE block medians must span at most 2% with the terminal median below `ln(128)`. Trust additionally requires clip fraction at most 0.20, ESS fraction at least 0.80, p95 coefficient L2 drift at most 0.05, at least 16 live operators, top-k overlap decline at most 0.05, zero nonfinite values, frozen-byte identity, and no unexpected gradients. PASS or FAIL is persisted atomically; only PASS may emit a candidate. The independently hardened promotion authenticator passes **57/57 focused and 117/117 combined prelaunch tests** against the refreshed source pin; it requires exact step-54,466/4,800-update/config/source lineage, all six folds plus validation provenance, and matching convergence/trust/terminal PASS evidence before it will accept a candidate. External promotion then requires at least 164/400 successes on untouched official seed-0 episodes and paired improvement over the 149/400 reference.

Validation job `32523490` completed `0:0` at **2026-08-20 00:14:48 UTC**. Its atomic COMPLETE manifest is SHA-256 **`c7a59139bfe4cc8412a82f735b9668f4865f753e5248957b3430617b3e6b9272`** and contains exactly 400 groups / 3,200 trajectories, with 291 informative groups versus the required 200. Terminal successes by arm are `[143,148,125,139,146,134,121,126]`; arm 0 is 143/400 and pass@8 is 309/400. Arm-0/pass@8 suite counts are Spatial `38/77`, Object `32/79`, Goal `48/88`, and Long `25/65`. The collector recorded zero harness errors and one nonfatal MuJoCo QACC instability warning.

Canonical trainer attempt **`32530592`** (`r0a_outcome_grpo_s1`) ran on eight ranks from 00:14:56 to 00:19:23 UTC and failed closed during pre-update behavior authentication (`FAILED 2:0`). Replay found parent-proposal coefficient mismatches in `libero_spatial/task=00/trial=10/seed=0 arm 0` (`max_abs=0.000567138`) and `trial=15 arm 0` (`max_abs=0.000200659`). That attempt used the exact unchanged config hash `1786ff2272566453` and then-current source closure `2efc943c81eec2250a46b0c5aa0007bd7c785a9b2081f6cf2ea1fa27390ae4a8`, but no model update was accepted: the run has zero metric rows, no HEARTBEAT/LATEST/checkpoint, no terminal report, and no candidate. This is a launch/authentication failure, not a training, convergence, trust, promotion, or SR result.

A full fixed-group A100-SXM4 replay probe, job **`32534089`**, completed `0:0` in 38 seconds (13.8998 seconds replay). Artifact `runs/outcome_auth_probe_train0_group0.json` is SHA-256 **`c028f191e6bca3d6b0cac915cdd9347cb0a952aaf9ace4caafecc6bd0eb255cd`** and covers all 469 replans of `train0` group 0. Collector-equivalent batch-1 FP32 with TF32 off and no autocast reproduced both stored coefficients and sampled old log-probabilities exactly: max/p95 errors were `0/0` for each, with raw argmax support order/set matching on `469/469` replans and arm-0 `97/97`; expanded-language and deterministic trainer batch-1 replays were also exact. Chunk-32 FP32 produced coefficient max/p95 `1.43018e-3/4.51460e-4` and sampled-old-log-probability max/p95 `7.94983e-3/3.35903e-3`, with sets matching `469/469` but one order change. Batch-1 TF32 increased those errors to `4.07127e-2/1.08394e-2` and `3.95479e-1/1.04792e-1`, and batch-1 BF16 to `5.62145e-1/1.16199e-1` and `2.72474/1.29072`, with support changes in both reduced-precision modes. The exact replay fix is therefore row-wise batch-1 FP32, TF32 off, no autocast, with chunking retained only for transfer and no tolerance widening. Config remains `1786ff2272566453`; the probe-validated row-wise source closure is `33223ca4dee84190fa9061fa48c6b7c2e488bce0a8338b01525034b2a41ed87b`, superseded for launch only by sole-writer hardening closure `61882a886642b0fc2b09ffdaf570ab1f53946f7cf52f4909d900f8368ada01a3`.

Corrected relaunches **`32534207`**/ **`32534324`** shared `runs/r0a_outcome_grpo_s1`; the duplicate-launch guard wrote `STOP` before the first update. All 16 ranks reported `PREEMPTED_DURING_COLLECTION_AUTH`, both jobs completed `0:0` at step 49,666, and the run has zero metric rows and no HEARTBEAT/LATEST/checkpoint/terminal report/candidate. A process-lifetime sole-writer lock was then added and authenticated under closure `61882a886642b0fc2b09ffdaf570ab1f53946f7cf52f4909d900f8368ada01a3`. Canonical job **`32534523`** (`r0a_outcome_grpo_canonical`) is active in isolated `runs/r0a_outcome_grpo_canonical_s1` and owns that lock. Submission `32534680` targets the old run directory and is superseded through its existing `STOP` sentinel. No training, convergence, trust, promotion, or SR result is claimed from any nonterminal canonical work.

The collided run and its `STOP` are preserved unchanged. A single canonical retry, job **`32534523`** (`r0a_outcome_grpo_canonical`), instead used the fresh unique directory `runs/r0a_outcome_grpo_canonical_s1`. It completed the full pre-update gate: row-wise batch-1 replay passed on `train0..train5` plus validation with atom counts `[119090,117310,118179,117895,119238,117099,240945]`, and every split reported exactly zero maximum coefficient and old-log-probability error under the original tight tolerances. All eight ranks opened the exact authenticated train loader (1,960 trajectories / 47,271 windows, uniform-task sampling, burn-in 4); W&B run ID is `b1a5a9a9752b4f45`. The durable initial checkpoint and `LATEST=49666` were written before training. The first accepted row then exposed a distinct fail-closed defect in the training path: under its bf16 autocast/batched replay, the supposedly identical behavior ratio was already `mean=1.05611`, `min=0.469427`, `max=2.21471`, with clip fraction `0.544643`, rather than identity. `STOP` was written with reason `fail closed: initial behavior ratio was not identity under bf16 autocast; preserve checkpoint for audit only`. The job completed `0:0` at **2026-08-20 01:17:43 UTC** after 133 finite, skip-free, config-matching contiguous rows (steps 49,667–49,799); terminal `LATEST=49799` points to the sentinel checkpoint (SHA-256 `94394a554cc676388dea65863e2ae7c2fe5687955c649de6ae54b986b0a39deb`, 586,209,834 bytes, one proposal reset, source closure `61882a886642b0fc2b09ffdaf570ab1f53946f7cf52f4909d900f8368ada01a3`). There is no terminal report or candidate. This checkpoint is audit-only: no convergence, trust, promotion, or simulator result is claimed.

Iteration 3a-QA-omega0 — **rejected at liveness.** `configs/r0a_bank_ca_qa_omega0.yaml`, resolved hash `7a5e8a24327ecc0c`, inherited QA unchanged and added one authenticated parameter transition from deploy hash `a199324a6205bb6d`: zero exactly `bank.omega` (6,291,456 elements). The release-gate implementation and integration slice passed 125/125. Before any training allocation began, `scripts/omega0_preflight.py` restored the verified step-49,666 deploy checkpoint, zeroed omega in memory, proved every other tensor and the complete estimator/proposal/decoder path bit-exact, and evaluated 16 fixed task-distinct windows from the authenticated 40-trajectory/918-window `demo_49` holdout. It passed all four strict mean comparisons: identity versus rollout was `0.00671727>0.00640048`, `0.01163689>0.01096431`, `0.01364525>0.01276358`, and `0.01669411>0.01586609`, with zero errors. Pre-update artifact: `runs/r0a_bank_ca_qa_omega0_s1/pre_update_gate.json` (`sha256:c4745ffa5459aaed3ba6d5b72db6f8a82b3f3176152aced5e61cdfce685a532e`). Training then ran from all 16 exact deploy shards under jobs `32522670/32522671/32522672`, with the expected one-time omega and bank/q_action optimizer resets. The exact 2,000-row `(50,666,52,666]` liveness gate failed only `delta_op`: median `+1.079626e-4` versus required `>+0.01`; median bank/q_action gradients were `4.67323e-4/1.41187e-2`, skips were zero, and no unexpected gradients occurred. Artifact: `runs/r0a_bank_ca_qa_omega0_s1/liveness_050666_052666.json` (`sha256:7ec6fe59aa36ca0811206c3db627ff44275f9d1ecebe08a0dd647dc71f5388c8`). Autostop wrote `STOP`. All three serialized jobs completed `0:0` without overlap at steps 52,675/52,676/52,677; the two same-hash recovery links did not repeat either reset, and their one-step sentinel races remained outside the fixed gate. Terminal `LATEST=52677` has 16/16 nonzero shards, and all 3,011 metric rows from 49,667 through 52,677 are valid and contiguous. No convergence, heldout-bank, oracle, or simulator result is claimed. This closes the offline bank/dynamics branch.

### Superseded Iteration 4 Round-0 planning note

The earlier one-fold/400-update Round-0 harness and its real-cache preflight remain a noncanonical diagnostic only. Job `32526232` auto-released when validation completed and `32528244` remains chained behind it, but neither can supply evidence for the canonical six-fold recipe. The authoritative recipe, evidence, gates, and status are the Iteration 4 entry above; do not promote or interpret a Round-0 artifact as its candidate.

### Iteration 4 exact-scoring correction and clean-relaunch preflight (2026-08-20 02:00 UTC)

Job `32534523` is terminal and rejected, not active. Its audit-only step-49,799 checkpoint contains 133 updates whose first stored-policy ratio was already nonidentity (`mean=1.05611`, `min=0.469427`, `max=2.21471`, clip fraction `0.544643`); it must never be resumed or evaluated. The method and data recipe are unchanged. The implementation correction makes every behavior/current-proposal comparison use the collector's exact execution contract: proposal `eval`, row-wise batch 1, FP32, autocast off, CUDA matmul and cuDNN TF32 off, highest FP32 matmul precision, and the stored Plackett–Luce order. Sparse-CE anchor computation alone retains bf16 autocast.

The final resolved recipe hash is **`a2ae23093778e803`** and the authenticated 28-file trainer closure is **`b3668bd22e1283248239725348e6b305fe94f411e708fbf6ccaa0673604c71e5`**. Before any scheduler, optimizer-zeroing, backward, or update, a fresh run must prove: all seven recovery splits replay every one of 949,756 recorded atoms with exactly zero coefficient/log-probability/log-ratio error; the step-49,666 proposal equals the authenticated parent and proposal Adam is empty/reset once; and all eight ranks' first production forwards have exact ratio 1, zero clipping, and the same scoring geometry. These reports are checkpointed, authenticated on resume and every convergence snapshot, and embedded in the candidate. Terminal evaluation assigns the seed plus five heldout snapshots to ranks 0–5, trust to rank 6, and idle verification to rank 7; all ranks must hold the identical final proposal, which must equal both the step-54,466 snapshot and emitted candidate digest.

The independent promotion gate now authenticates those exact behavior/start/first-forward/terminal witnesses in addition to the predeclared convergence and trust gates. Verification is green: **92/92** trainer-plus-direct-validation tests, **84/84** promotion tests, and **176/176** combined trainer/direct/promotion tests; Python compilation, both CLIs, shell syntax, source/config pin recomputation, and whitespace checks pass. The launcher requires an explicit fresh unique run directory, exports the deterministic cuBLAS workspace before Python, and refuses the old STOP-bearing or contaminated run directories. No corrected training update, candidate, convergence verdict, promotion result, or new SR is claimed yet.

Clean relaunch `32541821` is terminal **FAIL before update**, not a training result. It ran from 02:05:33–02:41:50 UTC (`FAILED 2:0`, 36:17) and replayed coefficients exactly on all seven splits, but the stored PL scalar differed by `7.6293945e-6`–`1.5258789e-5`. The run contains only its lock and resolved config: no anchor/W&B startup, START/LATEST checkpoint, metric row, optimizer update, terminal report, candidate, convergence, promotion, or SR evidence. The fault was smaller than the proposal forward: `stored_order_logprob` already produced each proposal row at B=1, then concatenated the rows and accumulated `pl_log_prob` at the transfer-batch size. The collector accumulated each stored PL atom at B=1.

The minimal correction keeps the method, data, stored behavior denominator, objective, and model inputs unchanged and accumulates stored-order PL row by row at B=1 under the same FP32/autocast-off/TF32-off geometry as collection. Replay and the first production forward remain literal identity gates: coefficient error `0`, old-logprob/log-ratio error `0`, ratio atoms exactly `14` per rank, ratios exactly `1`, ESS exactly `1`, and zero clipping before backward. The resolved fail-closed recipe is **`25afdedfc9deea5e`** and the 28-file trainer closure is **`7345b18448be1363e3e9dc3a8cd71741258ae906872305b4522d9cb4d7cb777c`**. The trainer/direct-validation/promotion/probe slice passes **195/195**, plus Python compilation, shell syntax, pin recomputation, and whitespace checks.

One-A100 read-only witness job **`32545881`** completed `0:0` in 34 seconds. Artifact `runs/outcome_ratio_identity_probe_7345b184.json` has SHA-256 **`c06d8106fe9e1aac6c498887355d459dc17f30a3fef08dc92db61b23d1759353`**. Its first authenticated train0 chunk contained 30 rows: the legacy batched PL accumulation reproduced three mismatches with maximum error `1.9073486e-6`, while the corrected row-wise path had exact `0` coefficient error and exact `0` stored-old-logprob error on every row. The production START sample then had 14/14 ratios exactly `1`, `max_abs_logratio=0`, zero clipping, finite proposal backward, and zero optimizer steps. This authorizes a fresh eight-GPU launch; no training or SR result is claimed by the probe.

Fresh canonical training job **`32545962`** started at 2026-08-20 03:08 UTC in the unique directory `runs/r0a_outcome_grpo_pl_b1_s1`, pinned to config **`25afdedfc9deea5e`** and source closure **`7345b18448be1363e3e9dc3a8cd71741258ae906872305b4522d9cb4d7cb777c`**. Its seven-split deep replay passed exactly over all 949,756 stored atoms (`[119090,117310,118179,117895,119238,117099,240945]` for `train0..train5,validation`): every split reported coefficient error `0`, old-logprob error `0`, and log-ratio error `0` under row-wise B=1 FP32 scoring. The step-49,666 START checkpoint then passed proposal identity with digest `62e98b6307026a6092b4e655c8148313aff5fac57fe5bb8a7e396d59a50d7aef`, one proposal-only optimizer reset, and zero optimizer-state entries; all eight authenticated anchor loaders opened the exact 1,960-trajectory/47,271-window train split. W&B run **`f071dc278f534e45`** is online. Before the first backward, every rank's 14 behavior ratios was exactly `1`, with ESS `1`, zero log-ratio, and zero clipping. As of 2026-08-20 03:49 UTC, training had accepted 20 contiguous proposal-only updates through step 49,686; all stored losses and gradients were finite, `grad_skipped=0`, and no unexpected-gradient violation was reported. This is authenticated live-training evidence only: no convergence, trust, candidate, promotion, simulator evaluation, or SR result is claimed.

At the user's request, job **`32545962`** was cancelled at 2026-08-20 04:51:21 UTC and is terminal `CANCELLED`; the queue was verified empty before relaunch. Its directory is preserved as audit-only with `LATEST=52325`, 2,659 exactly contiguous finite metric rows for steps 49,667--52,325, zero skipped gradients, no terminal report, and no candidate. It must not be resumed or evaluated. The corrected method was re-audited before a formal restart: the trainer/direct-validation/promotion/ratio-probe slice passed **195/195**, Python compilation, shell syntax, whitespace checks, recipe validation, and independent config/source-pin recomputation all passed. The partial run also preserved all eight exact first-forward identity witnesses. One reproducibility caveat is explicit: PyTorch reports nondeterministic Flash/Memory-Efficient attention backward under the repository's warn-only deterministic setting, so independent fresh runs are not guaranteed bitwise equality; this does not relax the exact collector/trainer forward identity or any terminal gate.

Formal fresh job **`32554776`** (`r0a_outcome_grpo_pl_b1_s2`) started at 2026-08-20 04:57 UTC in the previously absent directory `runs/r0a_outcome_grpo_pl_b1_s2`, with the same canonical config/source pins and an independent proposal-Adam reset from the authenticated step-49,666 parent. Its gated downstream chain is already submitted: official seed-0 end-to-end evaluation **`32554777`**, paired promotion gate **`32554778`**, and, only after promotion PASS, the formal seed-0/1/2 1,200-episode end-to-end SR evaluation **`32554779`**. No result is claimed while those jobs remain nonterminal.

The `s2` chain was subsequently cancelled before any optimizer update after a stricter reproducibility audit. Job `32554776` had created only its lock and resolved config—no `LATEST`, metric row, checkpoint, terminal report, or candidate—and dependent jobs `32554777`/`32554778`/`32554779` never started. The issue was not collector/trainer forward identity or within-run rank synchronization: `run.deterministic: true` was implemented through a shared `warn_only=True` helper, and the earlier production log showed one Flash and one Memory-Efficient nondeterministic-backward warning per rank. That permits fresh-run or interrupted-resume trajectories to drift and is too weak for the definitive formal lineage.

The outcome trainer now tightens deterministic algorithms locally to `warn_only=False` before either proposal loss path, without changing the shared repository helper, the YAML, collector geometry, objective, data, or config hash **`25afdedfc9deea5e`**. START identity, terminal provenance, and promotion authentication require the strict runtime state. The ratio probe also exercises both the exact FP32 GRPO/Switch backward and a real bf16 proposal-anchor backward, rejects nondeterminism warnings, and proves the proposal bytes remain unchanged without an optimizer step. The resulting authenticated 28-file trainer closure is **`d5ef53e9f2e276f17d68f80b4c081c8f09b0d89ea9a966214fc3b63387364a52`**. The focused trainer/direct-validation/promotion/probe slice passes **198/198**; Python compilation, shell syntax, whitespace checks, recipe validation, and independent config/source-pin recomputation pass.

One-A100 strict witness job **`32556325`** completed `0:0` in 36 seconds. Artifact `runs/outcome_ratio_identity_probe_d5ef53e9.json` has SHA-256 **`b00f18f0aa52892f77c2e2cb759cca1e98007fcbcc29d9dfb85eee26539bdb65`**. On an A100-SXM4-80GB with PyTorch 2.6.0+cu124, deterministic algorithms were enabled with warn-only false; corrected replay had exact zero coefficient and old-logprob error on all 30 rows; the production sample had 14/14 ratios exactly one, zero log-ratio/clipping, and ESS one. Both FP32 and bf16 proposal backwards had finite gradients and no nondeterminism warning, the proposal digest remained **`62e98b6307026a6092b4e655c8148313aff5fac57fe5bb8a7e396d59a50d7aef`**, and no optimizer step occurred.

The definitive strict lineage uses the new, previously absent directory `runs/r0a_outcome_grpo_strict_s1`. Formal training job **`32556444`** started at 2026-08-20 05:20:43 UTC with the strict source closure and unchanged config hash. Its gated downstream chain is official seed-0 evaluation **`32556445`**, paired promotion **`32556446`**, and full seed-0/1/2 1,200-episode evaluation **`32556447`**. The training log has already printed the exact strict runtime and collector-matching B=1 FP32 geometry; no training, candidate, promotion, or SR result is claimed until the corresponding artifacts and gates are terminally audited.

Pre-execution audit found that original promotion job `32556446` used Slurm's generated `#!/bin/sh` wrapper with the Bash-only command `set -euo pipefail`; this cluster's `/bin/sh` is dash, so the wrapper would have failed before invoking the gate. It and downstream `32556447` were cancelled while still dependency-held and with zero runtime. Their safe replacements are promotion **`32556683`** (`afterok:32556445`) using a POSIX-compatible `cd && python` wrapper and full 1,200-episode evaluation **`32556684`** (`afterok:32556683`). Training `32556444` and seed-0 evaluation `32556445` are unchanged.

Strict job `32556444` completed its pre-update gate at 2026-08-20 06:00 UTC. All seven recovery splits passed literal replay over **949,756** atoms with per-split counts `[119090,117310,118179,117895,119238,117099,240945]`; maximum coefficient, stored-old-logprob, and log-ratio errors were each exactly zero. The durable START checkpoint is step 49,666 with authenticated proposal digest **`62e98b6307026a6092b4e655c8148313aff5fac57fe5bb8a7e396d59a50d7aef`**, exactly one proposal-Adam reset, and zero optimizer-state entries. Every rank 0--7 then passed its first production forward on 14 ratio atoms: min/mean/max ratio exactly one, ESS one, zero log-ratio, and zero clipping. That gathered witness binds strict runtime `{deterministic_algorithms:true,warn_only:false}`, source closure `d5ef53e9f2e276f17d68f80b4c081c8f09b0d89ea9a966214fc3b63387364a52`, config `25afdedfc9deea5e`, and parent checkpoint `15f286c268caa5327d5aa3abf1f67ebd0555c426a509fef22cb7f537bf6ab4e1`. Fresh W&B run ID is **`f4a816726c0942e2`**. The first 37 accepted updates through step 49,703 were contiguous, finite, unskipped, and used the exact scoring geometry; critically, neither strict backward path emitted the former Flash or Memory-Efficient nondeterminism warning. This establishes formal training liveness only; terminal convergence, trust, candidate, promotion, and SR remain unclaimed.

The first durable post-update checkpoint at step 49,866 independently passed audit. `outcome_ckpt_000049866.pt` is 586,218,026 bytes with SHA-256 **`93cb5c0eec2186512feccc64c9e8f77b8ca7acb1ca278892e2e1f8dd6ec8dd04`** and is readable through both checkpoint load modes; `LATEST` points exactly to 49,866. Its header binds the correct outcome-training kind, config/source/parent identities, world size eight, empty stop reason, one proposal reset, strict START witness, and seven-split replay. All 121 proposal tensors are finite; all 121 proposal optimizer states are finite at Adam step 200 in the expected decay/no-decay groups. The durable metric prefix is exactly 200 contiguous rows for steps 49,667--49,866 and accepted updates 1--200, with correct config, finite values, and zero skipped gradients. The post-update proposal digest is `0244431e...0b201`. Training continued normally beyond this checkpoint; terminal evidence remains pending.

### Iteration 4 strict terminal result — rejected (2026-08-20 08:23 UTC)

Definitive strict job **`32556444`** completed the entire predeclared optimization window before failing closed at its terminal method gate. The ledger contains exactly **4,800** contiguous accepted updates for steps 49,667--54,466, six folds of 800 updates, 134,400 finite numeric leaves, zero skipped gradients, and no nondeterministic-kernel warning. The step-54,466 checkpoint is durable, but it is a trainer/resume artifact only. No consolidated candidate was emitted.

Atomic artifact `runs/r0a_outcome_grpo_strict_s1/terminal_report.json` has SHA-256 **`e42de09dcc0cf691c570f8a73b06144ad09767915565f876136d96fd36c94337`**, `status=FAIL`, `passed=false`, and `candidate_emitted=false`. Four predeclared predicates failed:

- held-out final-minus-seed efficacy estimate `-0.0165806`, 95% CI `[-0.0179273,-0.0152758]`, versus required lower endpoint `>0`;
- final mean approximate KL `0.0337827`, versus maximum `0.01`;
- final validation clip fraction `0.350272` (`74,050/211,407` sampled-arm atoms), versus maximum `0.20`;
- deployed arm-0 coefficient-drift p95 L2 `0.203810`, versus maximum `0.05`.

The failure is substantive rather than a crash or numerical-corruption event. All four final-versus-earlier plateau comparisons passed the declared `[-0.01,+0.01]` envelope, but every comparison was negative and the held-out mean surrogate worsened monotonically across the five terminal snapshots: `-0.0156907`, `-0.0178961`, `-0.0188745`, `-0.0205018`, and `-0.0224473` at steps 53,666--54,466, compared with seed `-0.00586670`. KL rose from `0.0222465` at the first predeclared snapshot to `0.0337827` at the final snapshot. The exact-update/CE checks, ESS (`0.927084`), all 128 live operators, top-k-overlap change (`-0.00317383`), finite-value checks, and frozen-gradient checks all passed.

Job `32556444` exited `FAILED 15:0` after 3:03:01 as the intended distributed TrustGateError propagation, not timeout. Dependency handling then cancelled official seed-0 evaluation **`32556445`**, replacement promotion **`32556683`**, and full 1,200-episode evaluation **`32556684`** with zero runtime. `candidate_000054466.pt`, promotion output, and all downstream SR result files are absent. No intermediate checkpoint is eligible for post-hoc promotion or simulator evaluation. An unchanged deterministic rerun would repeat the rejection; a future attempt requires a separately versioned, pre-frozen method/config/source/promotion contract and a genuinely fresh run directory.

### Iteration 4 post-failure diagnostics and v2 decision boundary (2026-08-20 09:37 UTC)

The failed strict run was long enough to answer its declared question, but its late terminal snapshots could not distinguish overtraining from a bad initial update direction. Read-only early-curve job **`32570932`** therefore authenticated four immutable checkpoints without performing an optimizer step, simulator episode, checkpoint selection, or candidate emission. It completed `0:0` in 40:02; artifact `runs/r0a_outcome_grpo_strict_s1_early_curve_diagnostic/summary.json` has SHA-256 **`620e3202a6ec170b9f240cba8aec801ef4370adfcf6d1b774b4ec249b985a27d`**. Seed-paired efficacy was already significantly negative at +200 updates: `-0.00242475`, 95% CI `[-0.00291176,-0.00194267]`, while coefficient-drift p95 was `0.133550` versus the `0.05` gate. The +400/+800/+1,600 checkpoints were also strictly negative, and every tested checkpoint failed at least coefficient drift. Thus neither the full 4,800-update horizon nor a predeclared shorter unchanged horizon is eligible for repetition.

Read-only component-gradient jobs then measured the exact authenticated seed without changing proposal bytes or optimizer state. The first instrumentation job `32571760` failed closed before evidence publication because its direct-versus-separate-bf16 additivity tolerance was unrealistically bitwise; the corrected diagnostic `32571877` completed `0:0` and established that sparse q_action CE has median gradient norm `22.6490` versus GRPO `5.80294`, while their cosine is approximately zero. Final projection job **`32572561`** completed `0:0` in 3:26; its format-v2 report `runs/diagnostics/outcome_component_gradient_audit/outcome_component_gradient_audit_v2_s49666_32572561.json` has SHA-256 **`8c9ea5c308fa232e2fb67be71cac853b49ff90193f05bd3b2f7ea87aeb288543`** and authenticated audit-source closure **`52af26cd386b3ff46788365e0b3f0df45626e9ef0bb478d474cfbd7a2f75af81`**. All 384 distinct training groups and 48 distinct development-validation groups were rehashed after use; strict B=1 identity, zero optimizer steps, empty optimizer state, unchanged proposal digest, and zero warnings passed.

Anchor rescaling alone is rejected. The GRPO component's clipped held-out directional derivative was harmful in four of six fold checks with mean `+0.232603`; GRPO plus balance at anchor weight zero remained harmful in four of six with mean `+0.223902`. Every predeclared anchor weight in `{0,0.1,0.25,0.5,0.75,1}` had a positive mean derivative and four or five harmful folds. Consequently the proposed v2 objective is **not frozen or launchable**. Its last admissible same-objective check is a no-update audit of the exact rank-distributed round-robin cross-fold direction; only a beneficial aggregate direction can authorize a bounded ineligible optimizer pilot. Otherwise the GRPO objective itself must be replaced rather than hidden by a smaller learning rate, more updates, a relaxed gate, or a zero-at-identity KL term.

The existing seed-0 recovery-validation collection is now explicitly development-only: v1 terminal scoring, the full early curve, and component projection have all informed the redesign. A future formal recipe must be frozen before collecting a new 400-group validation replicate (protocol seed 3, trials 40--49) and must bind its design-freeze receipt and new manifest identity. The official seed-0 trials 0--9 remain untouched for the final promotion SR gate.
